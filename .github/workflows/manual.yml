name: GitHub Actions Testing2
on:
  pull_request:
  workflow_dispatch:
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PR_URL: ${{ github.event.pull_request.html_url }}
jobs:
  job1:
    name: Explore-GitHub-Actions
    runs-on: ubuntu-latest
    outputs:
      sensitive: ${{ steps.diff.outputs.sensitive }}
    steps:
      - run: env
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Print api
        run: |
          echo ${{ github.api.url }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      - name: diffs
        id: diff
        run: |
          GIT_PR_DIFF=$(gh pr diff $PR_URL)
          echo "::set-output name=sensitive::false"
      - name: diff2
        if: contains($(gh pr diff $PR_URL), 'automatically')
        run: echo true

  job2:
    needs: job1
    name: Check diffs in PR
    runs-on: ubuntu-latest
    steps:
    - run: echo ${{needs.job1.outputs.sensitive}}
    - name: Print api
      run: |
        echo $GITHUB_API_URL
        echo $PR_URL
        GIT_PR_DIFF=$(gh pr diff $PR_URL)
        echo "---------------"
#        echo $GIT_PR_DIFF
    - name: hmm
      if: ${{ needs.job1.outputs.sensitive == 'true' }}
      run: echo true
    - name: hmm2
      if: ${{ needs.job1.outputs.sensitive == 'false' }}
      run: |
        echo false
        echo $GIT_PR_DIFF



